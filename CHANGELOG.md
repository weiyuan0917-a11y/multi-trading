# 更新日志

本文件用于记录每次版本更新的时间戳和主要内容。后续每次优化、修复或发布客户版时，都需要追加一条记录。

## V1.0.92 - 2026-06-24 16:49:17 +08:00

- Agent Strategy Lab 增强回测指标：新增平均单笔、平均盈利/亏损、盈亏比、Profit Factor、交易频率、日度 PnL 序列，并修复股票期权中长线粗略回测对 `pnl_usd` 的收益统计兼容。
- Agent Strategy Lab 新增候选相关性热力图，按每日 PnL 序列观察候选策略是否高度同质；研究中心股票策略矩阵同步新增 Top 标的收益分布与策略相关性热力图。
- 本次仅同步公开仓库文档记录，不提交自动交易执行相关源代码；开发端客户版构建脚本默认版本号已升级为 `1.0.92`。

## V1.0.68 - 2026-06-11 17:52:26 +08:00

- 同步客户版股票自动交易诊断接口字段：买入无信号时可记录入场规则、K线根数、策略输出、突破量能、RSI/均线偏离等元数据，便于排查 30 分钟 K 线模拟无信号。
- 开源客户仓库继续保持剔除完整自动交易 worker/页面的结构，仅同步已有轻量接口、规则元数据和文档记录。
- 客户版构建脚本默认版本号升级为 `1.0.68`，本次开发端已同步 `dist/customer` 和 `.iss`，后续可直接用 Inno 打包。

## V1.0.33 - 2026-06-02 16:19:54 +08:00

- 调整设置页账户区域布局：`Broker API 连接诊断（可视化）` 从账户管理上方移到账户管理下方，作为独立模块展示。
- 连接诊断模块改为默认折叠，仅点击“展开”后显示连接占用、上下文状态、建议操作和手动探测按钮。
- 账户管理中的注册账户表单默认隐藏，新增“添加账户”按钮；点击后才展开注册表单，注册成功或取消后自动收起。
- 同步客户版构建默认版本号为 `1.0.33`，需要重新生成客户版目录和 `.iss` 后再用 Inno 打包。

## V1.0.32 - 2026-06-02 13:28:21 +08:00

- 将 `MultiTradingLauncher.exe` 图标拆分为独立启动器图标：去掉英文文字，只保留左侧交易图形并放大，提升桌面/任务栏小尺寸识别度。
- 将 Inno 安装包图标拆分为独立安装器图标：主图标放大，并在右下角内部叠加下载箭头，避免箭头超出主图标边界。
- 客户版构建脚本分别使用 `multitrading-launcher.ico` 和 `multitrading-installer.ico`，后端 `Backend.exe` 继续保持 PyInstaller 原始输出，不做图标资源后处理。
- 同步客户版构建默认版本号为 `1.0.32`，需要重新生成客户版目录和 `.iss` 后再用 Inno 打包。

## V1.0.31 - 2026-06-02 11:46:00 +08:00

- 修复 V1.0.30 客户版安装包异常变小的问题：构建脚本不再对 PyInstaller 生成的 `Backend.exe` 做资源后处理，避免剥离 one-file 后端 exe 末尾附加的运行包体。
- 保留 `MultiTradingLauncher.exe` 和 Inno 安装包使用新版大图标；后端 exe 维持 PyInstaller 原始输出，保证客户版后端可正常启动。
- 同步客户版构建默认版本号为 `1.0.31`，需要重新生成客户版目录和 `.iss` 后再用 Inno 打包。

## V1.0.30 - 2026-06-02 11:25:35 +08:00

- 修复期权交易收益日历在开发端/客户端可能一直不显示的问题：兼容券商成交明细返回 `datetime` 或 ISO 时间字符串，避免成交时间被解析为 0 后整笔成交被丢弃。
- 优化“本年累计 / 本月累计”链路：累计请求改为 `summary_only` 轻量汇总，不再为累计数字重复返回整段日历格子和每日成交明细。
- 限制期权订单详情补充扫描按请求日期窗口取数，降低本年累计请求拉取历史订单详情导致的超时和页面卡住风险。
- 期权收益日历前端取消自动轮询，保留打开页面、切换月份/筛选和手动刷新时加载，避免多次刷新把后端请求堆积。
- 增加回归测试覆盖 `datetime` 成交时间、轻量汇总返回体，以及券商连接不可用时本地日志 fallback。
- 同步客户版构建默认版本号为 `1.0.30`。

## V1.0.29 - 2026-06-02 11:03:42 +08:00

- 将 Windows 软件图标源切换为 `assets/windows/multitrading-logo-source.png`，使用用户提供的大图标生成启动器和安装器图标。
- 重新生成多尺寸 `multitrading-logo.ico`，覆盖 16、24、32、48、64、128、256 像素，避免 exe 图标显示成横向小 logo。
- 客户版构建脚本会把同一图标应用到 `MultiTradingLauncher.exe`、`Backend.exe` 和可选自带安装器 exe，并在 Inno `.iss` 写入 `SetupIconFile`。
- 同步客户版构建默认版本号为 `1.0.29`；本次只同步客户版目录和 `.iss`，不运行 Inno 打包，不启动 worker。

## V1.0.28 - 2026-06-02 08:50:30 +08:00

- 修复客户版 V1.0.27 在券商连接熔断或 VPN/网络切换后，内部实时行情 `/internal/longport/quote` 把 `broker_connect_breaker_open` 打成 500 堆栈的问题；现在返回轻量 `available:false`，避免前端刷新风暴继续放大后端压力。
- 修复期权收益日历在券商暂时不可用时还没读取本地 worker ledger 就提前失败的问题；现在券商连接失败时仍会返回本地日志可还原的成交，并在 `debug` 标记券商连接不可用。
- 降低期权收益日历前端刷新频率，等待账户选择完成后再请求，拉长报表请求超时并关闭失败自动重试，减少页面多次刷新导致的预检/请求堆积。
- 日志排查确认 V1.0.27 客户端中 `/options/pnl-calendar` 只有 `OPTIONS` 预检、没有实际 `GET`，以及通知中心配置 BOM 触发过 `/notifications/status` 500；当前源码已保留 `utf-8-sig` 兼容和回归测试。
- 增加回归测试覆盖收益日历券商熔断 fallback，以及内部 Longbridge quote 熔断时不再 500。
- 同步客户版构建默认版本号为 `1.0.28`。

## V1.0.27 - 2026-06-02 07:18:08 +08:00

- 客户版启动器改为托管 Edge/Chrome App 模式窗口生命周期：关闭 MultiTrading 前端窗口后，会自动触发本地后端关停流程。
- 新增仅本机启动器可用的 `/internal/launcher/shutdown` 内部接口，通过一次性 launcher token 校验，不依赖用户登录态，避免窗口关闭后后端、前端和托管 worker 残留。
- 关停流程优先走后端 `stop-all` 正常路径，停止飞书机器人、股票自动交易、QQQ 0DTE、QQQ 1DTE、股票期权中长线 worker，再关闭后端；接口不可用时由启动器按本次启动的进程 PID 兜底清理。
- 启动器使用独立浏览器 profile 打开 App 窗口，提升窗口关闭检测可靠性，并记录 `launcher_lifecycle.log` 便于排查。
- 增加启动器关停 API 回归测试，覆盖无 token 拒绝、环境 token 和运行目录 token 文件授权场景。
- 同步客户版构建默认版本号为 `1.0.27`。

## V1.0.26 - 2026-06-01 17:33:02 +08:00

- 修复客户端股票自动交易自动买入时没有弹出 token 输入框、且未从当前用户 Setup 配置兜底读取 L3 `confirmation_token`，导致 `/trade/order` 返回 403 的问题。
- 统一股票自动交易服务端执行链路和 worker 代理下单链路：显式传入 token 优先，否则读取 `data/user_env/<用户>.env` 中的 `OPENCLAW_MCP_L3_CONFIRMATION_TOKEN`，仍由后端 L3 权限校验最终把关。
- 同步修补三个期权自动交易链路：`QQQ 0DTE`、`QQQ 1DTE`、`stock_options_swing` 在实盘下单时同样支持使用当前用户 Setup 保存的 L3 token 作为兜底。
- 修补股票期权中长线自动交易开启实盘自动提交时的 token 守卫，允许使用 Setup 中已配置的 L3 token，不再要求模块配置里重复填写。
- 增加回归测试覆盖股票自动交易、股票 worker 代理下单、0DTE/1DTE 期权下单、中长线期权下单和实盘开启守卫的 L3 token 兜底行为。
- 同步客户版构建默认版本号为 `1.0.26`。

## V1.0.25 - 2026-06-01 14:15:28 +08:00

- 修复 Agent Strategy Lab / 管理端候选验证读取服务器 K 线缓存时，长窗口缓存与短窗口缓存结束日期相差 1 天会被误判为 `kline_server_cache_incomplete` 的问题。
- 服务器 K 线缓存完整性判断允许 2 个自然日内的参考结束日偏差，兼容客户端和管理端缓存分批下载造成的轻微时间差。
- 保留真正陈旧长窗口缓存的 409 拦截，避免 LAB 验证误用明显落后的历史数据。
- 增加回归测试，覆盖长窗口比短窗口早 1 天但数据量完整的场景，以及现有陈旧/不完整缓存拦截场景。
- 同步客户版构建默认版本号为 `1.0.25`。

## V1.0.24 - 2026-06-01 11:59:07 +08:00

- 优化客户端整体 UI 视觉层：统一深色交易终端背景、面板、按钮、输入框、表格和顶部栏的设计 token。
- 主操作按钮从蓝紫渐变调整为更克制的青绿色交易主色，降低页面色彩噪声，提升专业感和可读性。
- 导航栏选中态改为轻量边框和左侧强调线，卡片/面板圆角统一收敛到 8px，减少不同页面之间的视觉割裂。
- 增加全局焦点态、滚动条、表格悬停态和小屏间距优化，减少交互控件的突兀感。
- 同步客户版构建默认版本号为 `1.0.24`。

## V1.0.23 - 2026-06-01 10:52:01 +08:00

- 修复股票自动交易确认下单时 L3 `confirmation_token` 只校验进程环境变量，可能与设置页保存的当前用户 token 不一致而报“无效或缺失”的问题。
- L3 权限校验改为优先读取当前 owner 的 `data/user_env/<用户>.env`，并兼容无 owner 的 worker 历史路径，覆盖股票自动交易和三个期权自动交易下单入口。
- 自动交易页面的 L3 token 在当前浏览器会话内临时保留，确认下单时若为空会先提示输入，避免误发空 token。
- 统一自动交易模块的 L3 就绪状态改为按当前用户设置判断，减少期权自动交易页面状态误报。
- 同步客户版构建默认版本号为 `1.0.23`。

## V1.0.22 - 2026-06-01 10:14:18 +08:00

- 期权交易收益日历底部汇总口径调整：原“月累计”改为“本年累计”，按当前自然年从 1 月 1 日统计到今天。
- 保留“本月累计”独立展示，方便同时查看自然年收益和当前月份收益。
- 客户版前端构建改用独立 `.next-customer` 输出目录，并在客户构建时降低静态生成并发，避免开发端 `.next\dev` 缓存或 Windows build worker 崩溃阻塞客户版目录同步。
- 同步客户版构建默认版本号为 `1.0.22`。

## V1.0.21 - 2026-06-01 10:03:56 +08:00

- 期权交易收益日历纳入股票期权中长线 `stock_options_swing` 实盘 ledger 的真实提交成交，避免只靠 0DTE / 日内目录兜底。
- `order_detail` 成交详情扫描改为增量补充，即使已有 worker 日志或 history executions，也会继续补全缺失的期权成交。
- 增加收益日历回归测试，覆盖中长线 ledger 解析和 order_detail 增量补充。
- 同步客户版构建默认版本号为 `1.0.21`。

## V1.0.20 - 2026-06-01 09:19:22 +08:00

- 修复客户版飞书定时市场报告未把本地 owner 透传给后端 API，导致定时线程可能走系统默认账户/空凭证并降级为兜底行情的问题。
- 定时市场报告的宏观环境和美股板块轮动优先复用后端 `/market/analysis`、`/market/sectors` 链路，与 Dashboard 使用同一套 owner 账户行情上下文。
- 当后端 API 临时不可用时，仍保留飞书 Bot 进程内本地分析回退，避免报告完全发送失败。
- 增加客户版飞书运行时回归测试，覆盖 owner 透传和定时报告 API 优先链路。
- 同步客户版构建默认版本号为 `1.0.20`。

## V1.0.19 - 2026-05-31 18:18:36 +08:00

- 客户版启动器优先使用 Edge/Chrome App 模式打开 MultiTrading，窗口不显示浏览器地址栏和标签页，更接近桌面软件体验。
- 若本机未找到 Edge/Chrome 或 App 模式启动失败，会自动回退到系统默认浏览器打开。
- 支持通过 `MULTITRADING_BROWSER_APP_MODE=0` 关闭 App 模式，通过 `MULTITRADING_BROWSER_EXE` 指定浏览器路径。
- 同步开发启动器和备用客户启动器的窗口打开逻辑，保持入口行为一致。
- 同步客户版构建默认版本号为 `1.0.19`。

## V1.0.18 - 2026-05-31 15:24:06 +08:00

- 修复股票期权中长线交易 Worker 状态误判：PID 文件必须匹配真实 worker 命令行，避免陈旧 PID 被 Windows 复用后显示为“运行中”。
- 停止股票期权中长线 Worker 时会清理陈旧 PID 和 runtime 文件，不会终止无关进程。
- 启动/停止自动交易模块后立即清理状态缓存，减少页面刷新后继续显示旧状态的问题。
- 股票期权中长线交易页面新增“刷新持仓”按钮，可手动读取券商期权持仓快照并展示账户原有未托管持仓。
- 手动刷新持仓快照不会启动 Worker，也不会提交订单；仅更新页面展示所需的 runtime 快照。
- 同步客户版构建默认版本号为 `1.0.18`。

## V1.0.17 - 2026-05-31 13:24:24 +08:00

- 移除通知中心的股票自动交易 Worker 启停入口，通知中心只保留状态展示和飞书管理。
- 保存自动交易配置、应用模板、导入/回滚配置和 Agent 参数更新时，不再自动同步拉起 Worker。
- Auto Trader 页面启动/重启股票自动交易 Worker 前增加二次确认。
- 默认自动交易配置改为关闭自动执行、开启演练模式，降低误触发实盘交易风险。
- 客户版运行环境显式关闭 API 启动时自动恢复股票自动交易 Worker。
- 同步客户版构建默认版本号为 `1.0.17`。

## V1.0.16 - 2026-05-31 11:24:40 +08:00

- 新增 `CHANGELOG.md`，用于记录每次版本更新的时间戳和主要内容。
- 在 README 标题区域增加更新日志入口，方便仓库页面直接查看更新记录。
- 明确后续每次优化、修复或发布客户版时，都需要同步升级版本号并追加更新记录。
- 优化网络不稳定或 VPN 切换时的券商连接处理。
- 增加同一券商账户建连并发保护，避免多次刷新导致重复连接、后端阻塞和登录超时。
- 对券商连接保护中、连接进行中状态增加更明确的错误识别和提示。
- 通知中心读取用户飞书配置增加短时间限频，减少频繁刷新时的后端压力。
- 前端同一 GET 请求增加进行中复用，减少重复请求。
- 已同步客户版目录和 Inno Setup `.iss` 脚本，未启动 worker，未触发实盘自动交易。
